# Create SSH keys
ssh-keygen

# Establish a SSH connection to every server to save the SSH fingerprint (one time only)
ssh <IP or hostname>

# Copy SSH keys to remote server(s)
ssh-copy-id <IP or hostname>
