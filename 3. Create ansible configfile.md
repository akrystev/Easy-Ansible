# Create INventory file to store information about server(s)
# Use your favourite text editor (I will use vim)
# Make sure that this file is in directory where ansible command would be run

vim ansible.cfg

[defaults]
# Set Inventory file location
inventory = hosts.ini
# Prompts for the SSH password
ask_pass = True

[privilege_escalation]
# Prompts for the sudo password (become password)
become_ask_pass = True
